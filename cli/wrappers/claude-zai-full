#!/bin/zsh
# Headroom-backed z.ai lane, rich/full profile. Requires the lane-scoped Headroom instance:
#   sb lane headroom zai
# (Resolved 2026-07-19 — see claude-zai for the root cause. Bypass: claude-zai-direct.)
exec sb run claude --with zai --rich "$@"
